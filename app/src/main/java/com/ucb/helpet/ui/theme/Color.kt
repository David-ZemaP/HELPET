package com.ucb.helpet.ui.theme

import androidx.compose.ui.graphics.Color


val DarkBackground = Color(0xFF111827) // Fondo principal de la pantalla
val CardBackground = Color(0xFF1F2937) // Fondo de tarjetas/contenedores
val InputBackground = Color(0xFF374151) // Fondo de campos de texto
val VioletIconBackground = Color(0xFF2E2E48) // Fondo circular para iconos

// Colores Principales (Violeta)
val VioletPrimary = Color(0xFFA78BFA)   // Violeta brillante (Textos destacados, iconos, bordes foco)
val VioletButton = Color(0xFF8B5CF6)    // Violeta sólido (Botones principales)

// Textos
val TextWhite = Color(0xFFF9FAFB)       // Texto principal (Títulos, labels)
val TextGray = Color(0xFF9CA3AF)        // Texto secundario (Subtítulos, placeholders)

// Colores Semánticos
val RedError = Color(0xFFEF4444)        // Errores
val GreenSuccess = Color(0xFF10B981)    // Éxito
val OrangeWarning = Color(0xFFF59E0B)   // Advertencias

// Neutros adicionales
val BorderColor = Color(0xFF374151)     // Color para bordes sutiles
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)